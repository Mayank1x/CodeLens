# Phase 1 dependencies — static analysis engine (no external APIs)
pytest>=8.0.0
pytest-cov>=5.0.0

# Phase 2 dependencies — LLM providers
google-genai>=1.0.0
# groq>=0.9.0 (We use the generic requests library for Groq REST API to minimize dependencies)
requests>=2.32.0

# Phase 3 dependencies — Flask API
flask>=3.1.0
flask-cors>=5.0.0
flask-limiter>=3.5.0
flask-sqlalchemy>=3.1.0
psycopg2-binary>=2.9.0
PyJWT>=2.8.0
python-dotenv>=1.0.1
gunicorn>=22.0.0

# Security — token encryption for stored GitHub OAuth tokens
cryptography>=43.0.0
